# Copy to `.dev.vars` for local `wrangler dev`. `.dev.vars` is gitignored.
# In production this value is a Worker SECRET, never a committed var:
#   npx wrangler secret put ANTHROPIC_API_KEY
ANTHROPIC_API_KEY=sk-ant-...
